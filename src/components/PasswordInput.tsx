import React from 'react';
import { render } from 'react-dom';

interface Props {
    value: string;
    classes:
}

export default const PasswordInput: React.FunctionComponent = (Props): string => {
    render() {
        return (

        )
    }
};
